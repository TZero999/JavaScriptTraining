let i= 0;
while (i<3)
{
    console.log (`number: ${i}`);
    ++i;
};
alert (`Готово. Итоговая цифра ${i-1}`);

/* Вообще я немного намудрил опять.
Вот пример из учебника:

let i = 0;
while (i < 3) {
  alert( `number ${i}!` );
  i++;
}

Моя проблема была в том, что i выводилась после цикла. И хоть в логе консоли всё было идеально, алерт выдавал 3-ку,
так как цикл успевал ещё один лишний раз увеличить значение переменной i. 
*/