/* Задание: Найдите неотрицательные целые числа

Есть строка с числами.
Создайте регулярное выражение, которое ищет только неотрицательные целые числа. 
Ноль разрешён.
*/

let str = "0 12 -5 123 -18 -125.65";
let regexp = /(?<![-.\d])\d+(?<![.])/g;

alert( str.match(regexp) ); // 0, 12, 123