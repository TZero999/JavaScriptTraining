alert (`JS считает, что 0 (Именно как строка, а не как число), преобразованный в Boolean, даёт True. Вот так: ${Boolean ('0')}`);

alert (`0 (именно как число 0, а не как строка), преобразованный в Boolean, даёт false. Вот так: ${Boolean (0)}`);

alert (`JS вообще считает любую не пустую строку, преобразованную в Boolean, как True. Например, даже пробел даст True. Вот так: ${Boolean(' ')}`);